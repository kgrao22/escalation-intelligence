node_modules/

# Machine-local Claude Code permissions (absolute paths)
.claude/settings.local.json
dist/
coverage/

# Fetched Slack data may contain customer information — never commit it
data/

# Secrets — never commit real credentials
.env
.env.local
.env.*.local

# Deployment-specific report presentation. Keys are real recurring-issue names
# produced against your own data — see display-names.example.json.
display-names.local.json

.DS_Store
*.log
